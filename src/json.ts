import { colors, type TokenColor } from "./colors";

export const json_tokenColors: TokenColor[] = [
    {
        scope: "support.type.property-name",
        settings: {
            foreground: colors.grey
        }
    }
]